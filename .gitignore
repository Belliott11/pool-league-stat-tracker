.DS_Store
Thumbs.db
*.local

# Real players' names and season data — kept local-only once the repo is public.
# Still referenced/used from README's "Starting from your existing Poolean data" section;
# it just isn't tracked in git anymore.
poolean-seed.json

# Real players' photos — same reasoning as poolean-seed.json above. Referenced by
# PLAYER_PHOTO_FILES in app.js and used from the local dashboard/dashboard-mail zips;
# just never pushed to this public repo's history.
photos/

# Adam's own repos, cloned locally for reference/research (each is its own separate git repo,
# not part of this one -- never meant to be committed here).
/poolean/
/poolvision/

# Shot-arc extraction prototype (standalone-shot-arc-plan.md): pretrained model weights
# (100MB+ each), extracted frame PNGs, and debug crops are all regenerable local scratch —
# none of it belongs in git history. The actual pipeline scripts (*.py) still get tracked.
shot-arc/frames/
shot-arc/*.pt
shot-arc/*.png
shot-arc/sample_shots.json
shot-arc/results.json
shot-arc/pose_results.json
shot-arc/labeling-manifest.js
shot-arc/*-labels.json
shot-arc/__pycache__/
